import { NextResponse } from "next/server";

export async function POST() {
  // TODO: create Stripe Checkout Session with user_id metadata and return URL
  return NextResponse.json({ status: "pending", message: "Not implemented" });
}

